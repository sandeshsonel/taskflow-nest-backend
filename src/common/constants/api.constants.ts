/**
 * API-related constants used across the application.
 * Centrally managed to ensure consistency in versioning and routing.
 */

export const API_PREFIX = process.env.API_PREFIX || 'api';
export const DEFAULT_API_VERSION = process.env.API_VERSION || '1';
